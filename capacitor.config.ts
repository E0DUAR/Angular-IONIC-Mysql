import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Angular-IONIC-Mysql',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
